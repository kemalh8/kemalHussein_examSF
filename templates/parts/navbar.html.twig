
<nav class="navbar navbar-expand-lg bg-body-tertiary ">
  <div class="container  ">
    <a class="navbar-brand" href="#">Navbar</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse   " id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item ">
                <a class="nav-link" href="{{path('app_default')}}">HOME</a>
            </li>
              <li class="nav-item">
                <a class="nav-link " aria-current="page" href="{{path('app_user_index')}}">LIST OF EMPLOYEES</a>
              </li>
          {% if is_granted('ROLE_RH')%}
                <li class="nav-item">
                <a class="nav-link" href="{{path("app_user_edit", {'id': app.user.id}) }}">UPDATE</a>
              </li>
                <li class="nav-item">
                <a class="nav-link" href="{{path("app_user_delete",{'id': app.user.id}) }}">DELETE</a>
              </li>
                <li class="nav-item">
                <a class="nav-link" href="{{path("app_user_new", {'id': app.user.id}) }}">REGISTER</a>
              </li>
          {% endif %} 
              
          {% if is_granted('IS_AUTHENTICATED_FULLY') %}
              <li class="nav-item">
                  <a class="nav-link" href="{{path('app_logout')}}">LOGOUT</a>
                </li>
            {% else %} 
              <li class="nav-item">
                <a class="nav-link" href="{{path('app_login')}}">LOGIN</a>
              </li>
          {% endif %}
            
            
       
      </ul>
    </div>
  </div>
</nav>
